# HardcodedApiKeyBan (Cursor plugin) — FREE forever

Fail CI on hardcoded API keys in string literals (sk_, AKIA, ghp_, xoxb_, JWT).

## Install
Install from Cursor marketplace / this repo. Plugin stays **free forever**.

## Upgrade for CI fail
Pro available · https://plugins.lawrence.dev/pro

## What it does
Runs `hardcodedapikeyban` patterns as guidance in the IDE. Full multi-file CI gate + SARIF is Pro (outside marketplace — no paywall in this plugin).
