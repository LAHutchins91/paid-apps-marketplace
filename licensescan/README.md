# LicenseScan (Cursor plugin) — FREE forever

Scan package.json / lockfile-ish inventory for license risk — GPL/AGPL flags, copyleft, proprietary.

## Install
Install from Cursor marketplace / this repo. Plugin stays **free forever**.

## Upgrade for CI fail
Pro available · https://plugins.lawrence.dev/pro

## What it does
Runs `licensescan` patterns as guidance in the IDE. Full multi-file CI gate + SARIF is Pro (outside marketplace — no paywall in this plugin).
