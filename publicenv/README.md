# PublicEnv (`publicenv`) (Cursor plugin) — FREE forever

Flag `NEXT_PUBLIC_` / `VITE_` secrets that look private — scan `.env*` for public-prefix keys whose names (or value shapes) scream secret.

## Install
Install from Cursor marketplace / this repo. Plugin stays **free forever**.

## Upgrade for CI fail
Pro available · https://plugins.lawrence.dev/pro

## What it does
Runs `publicenv` patterns as guidance in the IDE. Full multi-file CI gate + SARIF is Pro (outside marketplace — no paywall in this plugin).
