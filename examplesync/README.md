# ExampleSync (Cursor plugin) — FREE forever

Generate safe `.env.example` from live env contracts. Placeholders only — never copy real secret values into examples.

## Install
Install from Cursor marketplace / this repo. Plugin stays **free forever**.

## Upgrade for CI fail
Pro available · https://plugins.lawrence.dev/pro

## What it does
Runs `examplesync` patterns as guidance in the IDE. Full multi-file CI gate + SARIF is Pro (outside marketplace — no paywall in this plugin).
