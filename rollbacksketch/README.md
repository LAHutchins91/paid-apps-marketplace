# RollbackSketch (Cursor plugin) — FREE forever

Best-effort rollback sketches for SQL migrations — confidence labels, risk notes, and heavy warnings when a DROP cannot be reversed.

## Install
Install from Cursor marketplace / this repo. Plugin stays **free forever**.

## Upgrade for CI fail
Pro available · https://plugins.lawrence.dev/pro

## What it does
Runs `rollbacksketch` patterns as guidance in the IDE. Full multi-file CI gate + SARIF is Pro (outside marketplace — no paywall in this plugin).
