# PrismaGuard (Cursor plugin) — FREE forever

Lint Prisma schema & migrations for destructive DROP/delete risks — before they ship.

## Install
Install from Cursor marketplace / this repo. Plugin stays **free forever**.

## Upgrade for CI fail
Pro available · https://plugins.lawrence.dev/pro

## What it does
Runs `prismaguard` patterns as guidance in the IDE. Full multi-file CI gate + SARIF is Pro (outside marketplace — no paywall in this plugin).
