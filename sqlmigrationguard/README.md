# SqlMigrationGuard (Cursor plugin) — FREE forever

Review SQL migrations for destructive ops, lock hazards, and unsafe ordering — before they ship.

## Install
Install from Cursor marketplace / this repo. Plugin stays **free forever**.

## Upgrade for CI fail
Pro available · https://plugins.lawrence.dev/pro

## What it does
Runs `sqlmigrationguard` patterns as guidance in the IDE. Full multi-file CI gate + SARIF is Pro (outside marketplace — no paywall in this plugin).
