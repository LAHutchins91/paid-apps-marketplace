# JsonSchemaGuard (Cursor plugin) — FREE forever

Validate JSON/YAML payloads against JSON Schema — with a CI gate for API/platform teams.

## Install
Install from Cursor marketplace / this repo. Plugin stays **free forever**.

## Upgrade for CI fail
Pro available · https://plugins.lawrence.dev/pro

## What it does
Runs `jsonschemaguard` patterns as guidance in the IDE. Full multi-file CI gate + SARIF is Pro (outside marketplace — no paywall in this plugin).
