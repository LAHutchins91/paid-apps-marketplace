# ChildProcessShellBan (Cursor plugin) — FREE forever

Fail CI on exec(`${req.`) / spawn shell:true / execFile misuse.

## Install
Install from Cursor marketplace / this repo. Plugin stays **free forever**.

## Upgrade for CI fail
Pro **$39/mo** · Team available · https://childprocessshellban.dev

## What it does
Runs `childprocessshellban` patterns as guidance in the IDE. Full multi-file CI gate + SARIF is Pro.
