# Insecure Transport Ban

Fail CI on cleartext ws://, ftp://, http:// API bases, TLS reject disabled.

## What it catches

Fail CI on cleartext ws://, ftp://, http:// API bases, TLS reject disabled — surfaced as editor guidance so you can fix issues before they merge.

## Who it's for

Security-minded teams who want issues caught in the editor before CI.

## Install

Install **Insecure Transport Ban** from the Cursor marketplace, or open this folder as a local plugin.

## Free forever

This Cursor plugin stays **free forever**. No paywall in the plugin.

Want the full multi-file CI gate, SARIF, and team workflows? That’s **Pro** (outside Cursor):

→ https://plugins.lawrence.dev/pro

## License

MIT — see `LICENSE`.
