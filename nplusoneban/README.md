# NPlusOneBan (Cursor plugin) — FREE forever

Fail CI on ORM/DB calls inside loops (N+1).

## Install
Install from Cursor marketplace / this repo. Plugin stays **free forever**.

## Upgrade for CI fail
Pro available · https://plugins.lawrence.dev/pro

## What it does
Runs `nplusoneban` patterns as guidance in the IDE. Full multi-file CI gate + SARIF is Pro (outside marketplace — no paywall in this plugin).
