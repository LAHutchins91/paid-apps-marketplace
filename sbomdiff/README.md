# SbomDiff (Cursor plugin) — FREE forever

Compare two CycloneDX or SPDX JSON SBOMs. See added, removed, and upgraded packages. Paste the markdown report into a PR.

## Install
Install from Cursor marketplace / this repo. Plugin stays **free forever**.

## Upgrade for CI fail
Pro **$49/mo** · Team available · https://sbomdiff.dev

## What it does
Runs `sbomdiff` patterns as guidance in the IDE. Full multi-file CI gate + SARIF is Pro.
