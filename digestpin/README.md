# DigestPin (Cursor plugin) — FREE forever

Require digest-pinned container images in compose/k8s YAML — catch `:latest`, floating tags, and untagged images before they drift.

## Install
Install from Cursor marketplace / this repo. Plugin stays **free forever**.

## Upgrade for CI fail
Pro available · https://plugins.lawrence.dev/pro

## What it does
Runs `digestpin` patterns as guidance in the IDE. Full multi-file CI gate + SARIF is Pro (outside marketplace — no paywall in this plugin).
