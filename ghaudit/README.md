# ActionLint (`ghaudit`) (Cursor plugin) — FREE forever

Lint GitHub Actions workflow YAML for common footguns — unpinned actions (`@main`/`@master`), `pull_request_target` + checkout, secrets in `echo`, and missing `permissions:` (warn).

## Install
Install from Cursor marketplace / this repo. Plugin stays **free forever**.

## Upgrade for CI fail
Pro available · https://plugins.lawrence.dev/pro

## What it does
Runs `ghaudit` patterns as guidance in the IDE. Full multi-file CI gate + SARIF is Pro (outside marketplace — no paywall in this plugin).
